#pragma once
struct Access;
struct Sequence;
struct SeqList;
struct Sequence;

namespace models {

enum { INIV, PUTV, LOGV }; /* used in pangen1.c */
enum btypes { NONE, N_CLAIM, I_PROC, A_PROC, P_PROC, E_TRACE, N_TRACE };

struct Slicer;
struct Lextok;
struct Symbol;

} // namespace models